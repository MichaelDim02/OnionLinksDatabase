-- OLD v0.1
SELECT ALL FROM links WHERE tag1 = tag OR tag2 = tag OR tag3 = tag;
-- tag1, tag2, tag3 are the tag columns in the table links of the database
-- tag is the argument passed into the function (in the python script)

-- This is just a demonstration of the SQL code in case you are interested 
-- It is not linked with the program
-- It can be deleted / everything will work correctly without it
